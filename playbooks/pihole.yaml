---

# Configure pihole
- name: Configure pihole
  hosts: pihole
  gather_facts: false
  vars_files:
    - "{{ inventory_dir }}/../../vaults/{{ inventory_dir | basename }}/vault.yaml"
  roles:
    - pihole
  tags: pihole
